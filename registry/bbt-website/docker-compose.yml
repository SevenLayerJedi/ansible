version: "3.9"
services:
  webserver:
    container_name: bbt-webserver
    build:
      context: .
      dockerfile: Dockerfile
    volumes:
      - /mnt/nvme/www/html:/var/www/html
    ports:
      - 8000:80
